/**
 * Data structures module exports
 * Provides Trie, TrieNode, and MaxHeap classes for autocomplete functionality
 */

const Trie = require('./Trie');
const TrieNode = require('./TrieNode');
const MaxHeap = require('./MaxHeap');

module.exports = {
  Trie,
  TrieNode,
  MaxHeap
};